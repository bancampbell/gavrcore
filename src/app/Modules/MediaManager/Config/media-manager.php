<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Диск хранения
    |--------------------------------------------------------------------------
    |
    | Laravel Storage disk, на котором хранятся файлы медиа-менеджера.
    | По умолчанию: public (storage/app/public).
    |
    */
    'disk' => env('MEDIA_DISK', 'public'),

    /*
    |--------------------------------------------------------------------------
    | Базовый путь
    |--------------------------------------------------------------------------
    |
    | Корневая директория внутри диска, где хранятся загруженные файлы.
    | Не изменяйте после первой загрузки — иначе потеряете доступ к старым файлам.
    |
    */
    'base_path' => 'uploads',

    /*
    |--------------------------------------------------------------------------
    | Максимальный размер файла
    |--------------------------------------------------------------------------
    |
    | В килобайтах (KB). 100 * 1024 = ~100 МБ.
    |
    */
    'max_file_size' => 100 * 1024,

    /*
    |--------------------------------------------------------------------------
    | Максимальное количество файлов за одну загрузку
    |--------------------------------------------------------------------------
    |
    */
    'max_upload_files' => 20,

    /*
    |--------------------------------------------------------------------------
    | Разрешённые MIME-типы
    |--------------------------------------------------------------------------
    |
    | Список MIME-типов, которые разрешено загружать.
    | Проверка идёт по содержимому файла (mimetypes), а не по расширению.
    |
    */
    'allowed_mimetypes' => [
        'image/jpeg',
        'image/png',
        'image/gif',
        'image/svg+xml',
        'image/webp',
        'application/pdf',
        'application/msword',
        'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
        'application/vnd.ms-excel',
        'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
        'text/plain',
        'application/zip',
    ],

    /*
    |--------------------------------------------------------------------------
    | Время жизни кэша
    |--------------------------------------------------------------------------
    |
    | В секундах. 300 = 5 минут.
    |
    */
    'cache_ttl' => 300,

    /*
    |--------------------------------------------------------------------------
    | Максимальная глубина вложенности папок
    |--------------------------------------------------------------------------
    |
    | При превышении — сканирование дерева обрывается,
    | создание папки запрещается.
    |
    */
    'max_folder_depth' => 10,
];
